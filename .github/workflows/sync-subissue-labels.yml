name: Sync Parent Issue Labels to Sub-Issues

on:
  issues:
    types: [opened, edited, reopened]

jobs:
  sync_labels:
    runs-on: ubuntu-latest
    steps:
      - name: Sync Labels from Parent Issue
        uses: actions/github-script@v7
        with:
          script: |
            // Get the current issue
            const issue = context.issue;
            
            // Fetch issue details
            const { data: currentIssue } = await github.rest
